"use client";

import { useEffect, useMemo, useState } from "react";
import LinkThemeController from "@/components/LinkThemeController";

type Task={id:string;title:string;detail:string;source:string;money:boolean;done:boolean};
type State={date:string;tasks:Task[];body:boolean;mind:boolean;pocket:boolean;note:string};
const KEY="cc-personal-mission-v2";
const today=()=>new Date().toLocaleDateString("en-CA");
const fresh=():State=>({date:today(),tasks:[{id:"mission",title:"Cerrar una pieza concreta del ecosistema",detail:"Una sola pieza que debe quedar terminada hoy.",source:"MISIÓN PRINCIPAL",money:false,done:false}],body:false,mind:false,pocket:false,note:""});

export default function PersonalMission(){
 const [s,setS]=useState<State>(fresh()); const [title,setTitle]=useState(""); const [source,setSource]=useState("PERSONAL");
 useEffect(()=>{try{const raw=localStorage.getItem(KEY);if(raw){const p=JSON.parse(raw);if(p.date===today())setS(p)}}catch{}},[]);
 useEffect(()=>{localStorage.setItem(KEY,JSON.stringify(s))},[s]);
 const patch=(v:Partial<State>)=>setS(x=>({...x,...v}));
 const toggle=(id:string)=>patch({tasks:s.tasks.map(t=>t.id===id?{...t,done:!t.done}:t)});
 const remove=(id:string)=>patch({tasks:s.tasks.filter(t=>t.id!==id)});
 const add=()=>{if(!title.trim())return;patch({tasks:[...s.tasks,{id:crypto.randomUUID(),title:title.trim(),detail:"",source,money:source==="$ MONETIZACIÓN",done:false}]});setTitle("")};
 const open=s.tasks.filter(t=>!t.done),done=s.tasks.filter(t=>t.done),moneyDone=s.tasks.some(t=>t.money&&t.done),minimums=[s.body,s.mind,s.pocket].filter(Boolean).length;
 const dateLabel=useMemo(()=>new Date(`${s.date}T12:00:00`).toLocaleDateString("es-CL",{weekday:"long",day:"numeric",month:"long"}),[s.date]);
 return <main className="pm"><LinkThemeController/><style>{css}</style><div className="pm-shell">
  <header className="pm-header"><div><a href="/">← Control Central</a><small>MISIÓN PERSONAL</small><h1>Hoy</h1><p>{dateLabel} · {open.length} pendientes · {done.length} cerradas</p></div><div className="pm-status"><b>{minimums}/3</b><span>mínimos</span></div></header>

  <section className="pm-summary"><span className={moneyDone?"ok":""}>$ {moneyDone?"Movimiento comercial hecho":"Falta movimiento comercial"}</span><span>{minimums===3?"✓ Día sostenido":"Mantener el sistema vivo"}</span></section>

  <section className="pm-panel pm-list"><div className="pm-section-title"><h2>Hoy</h2><span>{open.length}</span></div>{open.map((t,i)=><TaskRow key={t.id} task={t} primary={i===0} toggle={()=>toggle(t.id)} remove={()=>remove(t.id)}/>)}</section>

  <section className="pm-add"><button className="pm-plus" onClick={add}>＋</button><input value={title} onChange={e=>setTitle(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="Añadir una tarea"/><select value={source} onChange={e=>setSource(e.target.value)}><option>PERSONAL</option><option>LINK DIGITAL</option><option>CARACOL</option><option>FLASH BOTOX</option><option>HOTEL EXPERIENCE</option><option>$ MONETIZACIÓN</option></select></section>

  <section className="pm-panel pm-minimums"><div className="pm-section-title"><h2>Mínimos de hoy</h2><span>{minimums}/3</span></div><Minimum title="Cuerpo" detail="Moverme y recuperar energía" checked={s.body} set={v=>patch({body:v})}/><Minimum title="Mente" detail="Definir qué cierro y no abrir frentes" checked={s.mind} set={v=>patch({mind:v})}/><Minimum title="Bolsillo" detail="1 movimiento comercial real" checked={s.pocket} set={v=>patch({pocket:v})}/></section>

  {done.length>0&&<details className="pm-panel pm-completed"><summary>Completadas <b>{done.length}</b></summary>{done.map(t=><TaskRow key={t.id} task={t} toggle={()=>toggle(t.id)} remove={()=>remove(t.id)}/>)}</details>}

  <section className="pm-close"><div><h2>Cierre del día</h2><p>Reduce antes de abandonar. Corrige sin volver a cero.</p></div><textarea value={s.note} onChange={e=>patch({note:e.target.value})} placeholder="Qué cerré, qué vendí y qué reduzco mañana…"/></section>
 </div></main>
}

function TaskRow({task,toggle,remove,primary=false}:{task:Task;toggle:()=>void;remove:()=>void;primary?:boolean}){return <div className={`pm-task ${task.done?"done":""} ${primary?"primary":""}`}><button className="pm-circle" onClick={toggle} aria-label="Completar">{task.done?"✓":""}</button><div className="pm-task-text"><b>{task.title}</b><div><span>{task.source}</span>{task.money&&<em>$ MONETIZACIÓN</em>}{task.detail&&<small>{task.detail}</small>}</div></div><button className="pm-more" onClick={remove} title="Eliminar">×</button></div>}
function Minimum({title,detail,checked,set}:{title:string;detail:string;checked:boolean;set:(v:boolean)=>void}){return <div className={`pm-task minimum ${checked?"done":""}`}><button className="pm-circle" onClick={()=>set(!checked)}>{checked?"✓":""}</button><div className="pm-task-text"><b>{title}</b><div><small>{detail}</small><span>↻ DIARIO</span></div></div></div>}

const css=`
.pm{min-height:100dvh;background:var(--link-bg);color:var(--link-text);padding:28px 20px 80px}.pm-shell{max-width:900px;margin:auto}.pm-header{display:flex;align-items:flex-end;justify-content:space-between;border-bottom:1px solid var(--link-line);padding:8px 2px 24px}.pm-header a{display:block;color:var(--link-muted);text-decoration:none;font-size:13px;margin-bottom:30px}.pm-header small{font-size:10px;letter-spacing:.16em;color:var(--link-muted)}.pm-header h1{font-size:46px;line-height:1;margin:7px 0 9px;font-weight:600;letter-spacing:-.045em}.pm-header p{margin:0;color:var(--link-muted);font-size:14px;text-transform:capitalize}.pm-status{display:flex;flex-direction:column;align-items:center;background:var(--link-surface);border:1px solid var(--link-line);border-radius:16px;padding:12px 20px;box-shadow:var(--link-shadow)}.pm-status b{font-size:20px}.pm-status span{font-size:11px;color:var(--link-muted)}.pm-summary{display:flex;gap:8px;padding:16px 0}.pm-summary span{font-size:12px;background:var(--link-surface);border:1px solid var(--link-line);border-radius:18px;padding:8px 12px;color:var(--link-muted)}.pm-summary .ok{background:var(--link-green-soft);color:var(--link-green);border-color:color-mix(in srgb,var(--link-green) 20%,var(--link-line))}.pm-panel{background:var(--link-surface);border:1px solid var(--link-line);border-radius:16px;padding:4px 18px;margin-bottom:12px;box-shadow:var(--link-shadow)}.pm-section-title{height:50px;display:flex;align-items:center;gap:8px}.pm-section-title h2{font-size:18px;font-weight:600;margin:0;letter-spacing:-.02em}.pm-section-title span{font-size:11px;color:var(--link-muted);background:var(--link-surface-2);padding:5px 8px;border-radius:999px}.pm-task{display:grid;grid-template-columns:32px 1fr 28px;align-items:flex-start;min-height:66px;padding:13px 0;border-top:1px solid var(--link-soft)}.pm-section-title+.pm-task{border-top:0}.pm-task.primary{background:var(--link-surface-2);margin:0 -10px;padding:15px 10px;border-radius:11px;border-top:0}.pm-circle{width:21px;height:21px;border-radius:50%;border:1.7px solid var(--link-muted);background:transparent;color:white;padding:0;font-size:12px;line-height:17px;margin-top:1px}.pm-task.primary .pm-circle{border-color:var(--link-text)}.pm-task-text{min-width:0}.pm-task-text>b{display:block;font-size:14px;font-weight:600;line-height:20px}.pm-task-text>div{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:4px}.pm-task-text span,.pm-task-text em{font-size:10px;font-style:normal;color:var(--link-blue);letter-spacing:.04em}.pm-task-text em{color:var(--link-green)}.pm-task-text small{font-size:11px;color:var(--link-muted)}.pm-more{border:0;background:transparent;color:var(--link-faint);font-size:18px}.done .pm-task-text>b{text-decoration:line-through;color:var(--link-muted)}.done .pm-task-text{opacity:.72}.done .pm-circle{background:var(--link-green);border-color:var(--link-green)}.pm-add{display:grid;grid-template-columns:38px 1fr 180px;background:var(--link-surface);border:1px solid var(--link-line);border-radius:14px;padding:8px 12px;margin-bottom:24px;box-shadow:var(--link-shadow)}.pm-plus{border:0;background:transparent;color:var(--link-text);font-size:24px}.pm-add input,.pm-add select{border:0;outline:0;background:transparent;color:var(--link-text)}.pm-add input{font-size:14px}.pm-add select{font-size:11px;color:var(--link-muted);text-align:right}.pm-minimums .minimum{grid-template-columns:32px 1fr}.pm-completed{padding-bottom:8px}.pm-completed summary{cursor:pointer;padding:14px 0;color:var(--link-muted);font-size:14px}.pm-completed summary b{float:right}.pm-close{margin-top:26px;border-top:1px solid var(--link-line);padding-top:22px;display:grid;grid-template-columns:1fr 1fr;gap:20px}.pm-close h2{font-size:18px;font-weight:600;margin:0 0 6px}.pm-close p{font-size:12px;color:var(--link-muted);margin:0}.pm-close textarea{min-height:90px;resize:vertical;border:1px solid var(--link-line);border-radius:11px;background:var(--link-surface);color:var(--link-text);padding:12px;font:13px var(--link-font);outline-color:var(--link-text);box-shadow:var(--link-shadow)}
@media(max-width:640px){.pm{padding:18px 12px 70px}.pm-header a{margin-bottom:22px}.pm-header h1{font-size:38px}.pm-status{padding:10px 14px}.pm-summary{overflow-x:auto}.pm-summary span{white-space:nowrap}.pm-panel{padding:4px 14px}.pm-add{grid-template-columns:36px 1fr}.pm-add select{grid-column:2;margin-top:7px;text-align:left}.pm-close{grid-template-columns:1fr}.pm-task-text small{width:100%}}
`;
